#!/bin/sh

TZS="UTC Europe/Budapest America/Los_Angeles America/New_York"

DATE="${1:-now}"
shift

date -d "$DATE" "$@"

for TZ in $TZS; do
  (
    echo "$TZ: "
    TZ=$TZ date -d "$DATE" "$@"
  ) | xargs
done
